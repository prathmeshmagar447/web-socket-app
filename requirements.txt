# Enhanced Web Socket Application Dependencies
# ===========================================

# Security and Encryption
cryptography>=41.0.0       # For message encryption and security
PyJWT>=2.8.0               # For JWT token authentication
bcrypt>=4.0.0              # Alternative password hashing (backup)

# Web Framework
Flask>=2.3.0               # Web interface framework
Flask-SocketIO>=5.3.0      # WebSocket support for Flask
Flask-CORS>=4.0.0          # Cross-origin resource sharing

# File Handling and Media
Pillow>=10.0.0             # Image processing and thumbnails
pydub>=0.25.0              # Audio file processing

# Notifications
plyer>=2.1.0               # Cross-platform notifications

# Development and Testing (optional)
pytest>=7.4.0             # Testing framework
black>=23.0.0              # Code formatting
flake8>=6.0.0              # Code linting

# Core Python modules (built-in, no installation needed):
# - socket (network communication)
# - sqlite3 (database operations) 
# - threading (concurrent operations)
# - json (data serialization)
# - datetime (timestamps)
# - os, sys (system operations)
# - hashlib, secrets (cryptographic functions)
# - base64 (encoding)
# - enum (enumerations)
